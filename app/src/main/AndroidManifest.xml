<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dicoding.picodiploma.covid19">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Covid19">
        <activity
            android:name=".webNews.WebNewsActivity"
            android:label="@string/title_activity_web_news">

        </activity>
        <activity
            android:name=".detailNews.DetailNewsActivity"
            android:label="@string/title_activity_detail_news"
            android:theme="@style/Theme.Covid19.NoActionBar" />

        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Covid19.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>